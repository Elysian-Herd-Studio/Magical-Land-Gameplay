# 震动感知：宽色团、边缘提示与过渡声

日期：2026-09-10。

## 调整范围

- 色团横向扩大约 35%，高度与活动脉冲沿用上一轮。
- 镜外目标用同一柔边色团贴边提示，进入画面后回到目标投影位置。取消旧方向点和左上角颜色图例，保留专注状态文字。
- 新增两段约 0.65 秒的程序合成过渡声：进入由快渐慢，退出由慢渐快。遵循主音量与玩家声音音量，世界声音速度不变；生成脚本当时随仓库保存，现由本地维护。
- 进入声等待服务器确认；重复确认不重播，失败请求不播放。退出与再次开启会替换前一段声音，断线和换维度静默清理。
- 专注期间临时增加约 6° FOV，转向灵敏度降至原来的 85%；随专注效果进退，不改写玩家设置，界面光标不受影响。

仅调整 Gameplay 客户端表现。感知范围、地面连通、目标分类、协议和种族规则沿用上一轮；外观包未改动。

## 验证与验收

本轮离线检查已通过：

- 全部 94 个 Gameplay 生产源码通过 Java 17 目标类型检查，仅依赖外观公共 API；原有种族、轮盘和出窍边界回归通过。
- 投影数学覆盖横向扩宽、完整转向、近裁剪面、上下看和后方接缝。镜外沿边框平滑移动，同一帧复用一份位置结果，避免重复推进或提示叠加。
- 实际 OpenGL 3.2 的有／无采样器扩展路径通过软边、屏幕边缘、颜色、透明度和深度检查。界面层降级绘制另有数学与源码检查。
- 声音资源通过真实 Minecraft OGG 解码和无声 OpenAL 回环检查。取消后晚到的解码保持静音，声源能正常结束并回收；重新生成资源的字节结果一致。
- 实际会话与消息编解码覆盖确认、失败、重复回包、快速切换和换维度。修正新维度终止包先于客户端换世界清理时可能误播退出声的问题。
- FOV 与鼠标检查覆盖渐变、边界、作用范围，以及真实 Minecraft 的世界／手持物 FOV 和最终转向调用位置。默认平滑视角、反向鼠标和望远镜处理保留。

相关入口为 `tests/sense/run-render-tests.ps1`、`tests/sense-transition/run-tests.ps1` 和 `tests/sense-client/run-tests.ps1`。这些检查不等同于游戏内运行全部新挂接，也不代替第三方模组组合与实际观感验收。

当时未覆盖色团宽度与转向衔接的实机观感、进退音效的节奏和清理，以及视野、转向减速、界面鼠标操作与退出恢复的手感。

2026-09-10 22:12（本地时间）完成独立快照构建并启动，沿用已备份的 Player565 存档、设置与外观依赖。日志确认资源和声音初始化完成，无新增启动错误；当时未记录实机效果验收结果，也未提交或推送。
