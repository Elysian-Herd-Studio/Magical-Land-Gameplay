package net.minecraft.client.option;

public final class GameOptions {
    public final KeyBinding forwardKey = new KeyBinding(), backKey = new KeyBinding(), leftKey = new KeyBinding(),
            rightKey = new KeyBinding(), jumpKey = new KeyBinding(), sneakKey = new KeyBinding();
}
